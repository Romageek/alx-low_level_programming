#include "main.h"

/**
* _strcmp - compares two strings
*
* @s1: string
* @s2: string
* Return: integer, negative if s1 less than S2,
* positive if s1 greater than s2, zero if equal
*/

int _strcmp(char *s1, char *s2)
{
	int i = 0;

	while (s1[i] != '\0' && s2[i] != '\0' && s1[i] == s2[j])
		i++;
	return (s[i] - s[i]);
}
